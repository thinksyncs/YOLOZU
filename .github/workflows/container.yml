name: container

on:
  push:
    tags:
      - "v*"
      - "release/v*"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  cpu:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: yolozu
            dockerfile: deploy/docker/Dockerfile
            title: YOLOZU
            description: YOLOZU CLI + packaged resources (CPU minimal; no torch).
          - image: yolozu-demo
            dockerfile: deploy/docker/Dockerfile.demo
            title: YOLOZU demo
            description: YOLOZU demo image (includes torch extra; CPU OK).
    steps:
      - uses: actions/checkout@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute image tags
        id: tags
        shell: bash
        run: |
          set -euo pipefail
          image="${REGISTRY}/${{ github.repository_owner }}/${{ matrix.image }}"

          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            ref="${GITHUB_REF_NAME}"
            version="${ref#v}"
            {
              echo "tags<<EOF"
              echo "${image}:${version}"
              echo "${image}:${ref}"
              echo "${image}:latest"
              echo "EOF"
            } >> "${GITHUB_OUTPUT}"
          else
            short="${GITHUB_SHA::12}"
            {
              echo "tags<<EOF"
              echo "${image}:sha-${short}"
              echo "EOF"
            } >> "${GITHUB_OUTPUT}"
          fi

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          labels: |
            org.opencontainers.image.title=${{ matrix.title }}
            org.opencontainers.image.description=${{ matrix.description }}
            org.opencontainers.image.licenses=Apache-2.0
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  runpod:
    if: ${{ secrets.NGC_API_KEY != '' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: yolozu-trt
            dockerfile: deploy/runpod/Dockerfile
            title: YOLOZU TRT tools
            description: YOLOZU tooling image for TensorRT/ONNXRuntime exporters (Linux + NVIDIA stack).
          - image: yolozu-rtdetr-pose
            dockerfile: deploy/runpod/Dockerfile.rtdetr_pose
            title: YOLOZU RTDETRPose
            description: YOLOZU RTDETRPose backend suite image (torch + onnxruntime-gpu).
    steps:
      - uses: actions/checkout@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to NVIDIA NGC (nvcr.io)
        uses: docker/login-action@v3
        with:
          registry: nvcr.io
          username: $oauthtoken
          password: ${{ secrets.NGC_API_KEY }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute image tags
        id: tags
        shell: bash
        run: |
          set -euo pipefail
          image="${REGISTRY}/${{ github.repository_owner }}/${{ matrix.image }}"

          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            ref="${GITHUB_REF_NAME}"
            version="${ref#v}"
            {
              echo "tags<<EOF"
              echo "${image}:${version}"
              echo "${image}:${ref}"
              echo "${image}:latest"
              echo "EOF"
            } >> "${GITHUB_OUTPUT}"
          else
            short="${GITHUB_SHA::12}"
            {
              echo "tags<<EOF"
              echo "${image}:sha-${short}"
              echo "EOF"
            } >> "${GITHUB_OUTPUT}"
          fi

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          labels: |
            org.opencontainers.image.title=${{ matrix.title }}
            org.opencontainers.image.description=${{ matrix.description }}
            org.opencontainers.image.licenses=Apache-2.0
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
